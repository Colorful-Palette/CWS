<Window x:Class="CWS.FloatingBall"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CWS Floating Ball" 
        Height="70" Width="70"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border x:Name="MenuBorder"
                                Background="#001F3F"
                                BorderBrush="#00BCF2"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Grid x:Name="ItemRoot">
                            <Border x:Name="Bg" Background="Transparent" SnapsToDevicePixels="True" CornerRadius="2"/>

                            <Rectangle x:Name="TopLine" Fill="#335577" Height="1" VerticalAlignment="Top" Visibility="Collapsed" Margin="5,0,5,0"/>

                            <Grid Margin="5,0,10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Ellipse x:Name="CheckMark" 
                                 Width="6" Height="6" 
                                 Fill="#00BCF2" 
                                 VerticalAlignment="Center" 
                                 HorizontalAlignment="Center" 
                                 Visibility="Collapsed">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" Color="#00BCF2" ShadowDepth="0"/>
                                    </Ellipse.Effect>
                                </Ellipse>

                                <ContentPresenter Grid.Column="1" 
                                          ContentSource="Header" 
                                          VerticalAlignment="Center" 
                                          HorizontalAlignment="Left"
                                          Margin="5,0,0,0"/>

                                <Path x:Name="Arrow" Grid.Column="2" Data="M0,0 L4,4 L0,8 Z" Fill="White" VerticalAlignment="Center" Margin="10,0,0,0" Visibility="Collapsed"/>
                            </Grid>

                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Right">
                                <Border Background="#001F3F" BorderBrush="#00BCF2" BorderThickness="1" CornerRadius="4" Padding="2">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#FF0078D4"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Header" Value="退出程序">
                                <Setter TargetName="TopLine" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="BreathingAnimation">
            <DoubleAnimation Storyboard.TargetName="OuterGlow" 
                             Storyboard.TargetProperty="BlurRadius"
                             From="8" To="20" Duration="0:0:1.5" 
                             AutoReverse="True" RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="OuterGlow" 
                             Storyboard.TargetProperty="Opacity"
                             From="0.4" To="0.8" Duration="0:0:1.5" 
                             AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource BreathingAnimation}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid x:Name="RootGrid" MouseLeftButtonDown="RootGrid_MouseLeftButtonDown" Background="Transparent" Cursor="Hand">
        <Ellipse x:Name="MainEllipse" 
                 Fill="#CC001F3F" 
                 Stroke="#00BCF2" 
                 StrokeThickness="2" 
                 Width="66" Height="66"
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Center">
            <Ellipse.Effect>
                <DropShadowEffect x:Name="OuterGlow" BlurRadius="10" Color="#00BCF2" ShadowDepth="0" Opacity="0.5"/>
            </Ellipse.Effect>
        </Ellipse>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
            <TextBlock Text="CWS" 
                       Foreground="White" 
                       FontWeight="Bold" 
                       FontSize="13"
                       Height="18" Width="30"/>
            <TextBlock Text="RUNNING" 
                       Foreground="#AA00BCF2" 
                       FontWeight="Normal" 
                       FontSize="8"
                       Margin="0,-1,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>